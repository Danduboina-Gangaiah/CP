BUBBLE SORT:
we need to compare the sibling elements and finding the max element at every time for n times 
we will get the max element every time



import java.util.*;
import java.lang.*;
import java.io.*;

class Codechef
{
    public static void bubbleSort(int arr[])
    {
        int n=arr.length;
        for(int i=0;i<n;i++)
        {
            for(int j=0;j<n-1-i;j++)
            {
                if(arr[j]>arr[j+1])
                {
                    int temp=arr[j];
                    arr[]=arr[j+1];
                    arr[j+1]=temp;
                }
            }
        }
    }
	public static void main (String[] args) throws java.lang.Exception
	{
		int arr[]={7,6,5,4,3,2};
		bubbleSort(arr);
		System.out.println(Arrays.toString(arr));

	}
}
